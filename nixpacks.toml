[providers]
  python = { version = "3.11" }

[phases.setup]
nixPkgs = ["pango", "cairo", "gdk-pixbuf", "gobject-introspection", "shared-mime-info", "fontconfig", "zlib"]

[start]
cmd = "uvicorn app.main:app --host 0.0.0.0 --port $PORT"
